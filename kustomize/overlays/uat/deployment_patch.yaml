apiVersion: apps/v1
kind: Deployment
metadata:
  name: caddy-deployment
spec:
  template:
    spec:
      containers:
        - name: caddy
          envFrom:
            - secretRef:
                name: caddy-env-uat
